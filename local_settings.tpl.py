# Path to your repository with the .git path
REPOSITORY_PATH = '/my/example/repository/path'

# List of all paths which have to be searched for analytics
SEARCHING_PATHS = (
    '',
)

# Endings of files which are in the searching paths and should be
# includes in the analytics process
ALLOWED_ENDINGS = (
    'py',
)

# List of regex patterns of files or paths which should not be included
# in the analytics
EXCLUDE_PATTERNS = (
    '',
)

# Lists of static paths which should be excluded from the analytics
EXCLUDE_PATHS = (
    '',
)